import { startScheduler } from '../services/onboardingScheduler.js';

console.log('🧪 TEST DU SCHEDULER DE SWAP QUOTIDIEN');
console.log('=====================================');

// Démarrer le scheduler (il va lancer un test immédiat)
startScheduler();